  <link href="{{ settings.static_path }}/css/star-rating.min.css" rel="stylesheet">
  <script src="{{ settings.static_path }}/css/star-rating.min.js"></script>
<div class="container1">
  <div class="tab-pane fade active in" id="mechanic">
    <div class="tab-pane" id="tabsleft-tab1">
        <table class="table table-striped">
          <thead>
            <tr>
              <th>Date</th>
              <th>reason</th>
              <th>project</th>
              <th>method</th>
              <th>Amount </th>
            </tr>
          </thead>
          <tbody>
            {{#each transactions }}
              <tr>
                <td>{{date ./date}}</td>
                <td>{{reason}}</td>
                <td> <a href='/projects/{{project.number}}'>{{project.description}}</a> </td>
                <td>{{method}}</td>
                <td>${{amount}}</td>
              </tr>
            {{/each }}
          </tbody>
        </table>
    </div>
  </div>
</div><!--Container closed-->
